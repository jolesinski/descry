model:
    name: object_13
    keypoints:
        type: orb
        max-features: 100

scene:
    name: T_07_willow_dataset
    keypoints:
        type: orb
        max-features: 1000

metrics:
    radius: 0.01
    visualize: true