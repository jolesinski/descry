model:
    name: object_13
    keypoints:
        type: orb
        max-features: 1000

scene:
    name: T_07_willow_dataset
    descriptors:
        type: orb
        max-features: 5000

metrics:
    radius: 0.01
    visualize: true