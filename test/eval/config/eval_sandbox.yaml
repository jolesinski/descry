model:
    name: object_13
    keypoints:
        type: orb
        max-features: 1000

scene:
    name: T_07_willow_dataset
    descriptors:
        type: orb
        max-features: 5000

matching:
    type: brute-force
    norm: hamming
    use-lowe: true
    lowe-ratio: 0.8

alignment:
    ransac-inlier-threshold: 5

metrics:
    radius: 0.01
    visualize: true